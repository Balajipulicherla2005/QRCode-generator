📱 QR Code Generator (Python)

This is a Python program that generates a QR code based on user input data.
The generated QR code is saved as an image file and can be scanned to view the stored information.

🚀 Features

Takes user details through input

Stores data in dictionary format

Generates a QR code from the data

Saves the QR code as a PNG image

Displays the QR code after generation

🛠️ Requirements

Python 3.7 or higher

qrcode library

Install the required library using:

pip install qrcode[pil]

📂 Project Structure
qr-code-generator/
│
├── qr_generator.py
└── README.md

🧪 How to Run

Open a terminal or command prompt

Navigate to the project folder

Run the program:

python qr_generator.py
